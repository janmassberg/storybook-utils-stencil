import { Meta, Source } from "@storybook/blocks";
import {
    generatorIntegrationArgs,
    generatorIntegrationSourceArgs,
} from "./generator.templates";
import { generateSourceCodeVue } from "../../src";

<Meta title="Code Generators/Vue" />

# Vue Source Generators

## Vue Component

Use `generateSourceCodeVue` to generate an integration example for Vue.

<Source
    code={`
import { generateSourceCodeVue } from "@jmassberg/storybook-utils";\n
const vueComponentSource = generateSourceCodeVue(${generatorIntegrationSourceArgs});
`}
/>

## Output

<Source
    dark
    language="html"
    code={generateSourceCodeVue(generatorIntegrationArgs)}
/>
