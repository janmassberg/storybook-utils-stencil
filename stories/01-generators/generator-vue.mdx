import { Meta, Source } from "@storybook/blocks";
import {
    generatorIntegrationArgs,
    generatorIntegrationSourceArgs,
} from "./generator.templates";
import { generateSourceVueComponent } from "../../src";

<Meta title="Code Generators/Vue" />

# Vue Source Generators

## Vue Component

Use `generateSourceVueComponent` to generate an integration example for Vue.

<Source
    code={`
import { generateSourceVueComponent } from "@jmassberg/storybook-utils";\n
const vueComponentSource = generateSourceVueComponent(${generatorIntegrationSourceArgs});
`}
/>

## Output

<Source
    dark
    language="html"
    code={generateSourceVueComponent(generatorIntegrationArgs)}
/>
