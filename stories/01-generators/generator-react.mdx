import { Meta, Source } from "@storybook/blocks";
import {
    generateSourceCodeAngularComponent,
    generateSourceCodeAngularHtml,
    generateSourceCodeHtml,
    generateSourceCodeJavascript,
    generateSourceCodeReact,
} from "../../src";
import { testArgs } from "../../src/test-utils/testArgs";

export const usageExample = `
import { generateSourceCodeReact } from "@jmassberg/storybook-utils-stencil";\n
const args = {
    testFalse: false,
    testTrue: true,
    testString: "test-string",
    testNumber: 12345,
    testArray: [1, 2, 3],
    testObject: {
        foo: "bar",
    },
    onTestEvent: testArgEventHandler,
    _children: "Test children",
};\n
const reactCode = generateSourceCodeReact("my-component", args);
`;

<Meta title="Code Generators/React" />

# React Source Code Generator

## Usage

Use `generateSourceCodeReact` to generate an integration example for React.

<Source code={usageExample} />

## Output

<Source
    dark
    language="jsx"
    code={generateSourceCodeReact("my-component", testArgs)}
/>
