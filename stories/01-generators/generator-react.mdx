import { Meta, Source } from "@storybook/blocks";
import {
    generatorIntegrationArgs,
    generatorIntegrationSourceArgs,
} from "./generator.templates";
import {
    generateSourceCodeReact,
    generateSourceCodeReactComponent,
} from "../../src";

<Meta title="Code Generators/React" />

# React Source Code Generator

## React JSX

Use `generateSourceCodeReact` to generate the React component JSX source.

<Source
    code={`
import { generateSourceCodeReact } from "@jmassberg/storybook-utils";\n
const reactJsxSource = generateSourceCodeReact(${generatorIntegrationSourceArgs});
`}
/>

### Output

<Source
    dark
    language="jsx"
    code={generateSourceCodeReact(generatorIntegrationArgs)}
/>

## React Function Component

Use `generateSourceCodeReactComponent` to generate an example React component integration source.

<Source
    code={`
import { generateSourceCodeReactComponent } from "@jmassberg/storybook-utils";\n
const reactFCSource = generateSourceCodeReactComponent(${generatorIntegrationSourceArgs});
`}
/>

### Output

<Source
    dark
    language="jsx"
    code={generateSourceCodeReactComponent(generatorIntegrationArgs)}
/>
