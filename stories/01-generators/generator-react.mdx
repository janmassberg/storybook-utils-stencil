import { Meta, Source } from "@storybook/blocks";
import {
    generatorIntegrationArgs,
    generatorIntegrationSourceArgs,
} from "./generator.templates";
import { generateSourceReactJsx, generateSourceReactFC } from "../../src";

<Meta title="Code Generators/React" />

# React Source Generators

## React JSX

Use `generateSourceReactJsx` to generate the React component JSX source.

<Source
    code={`
import { generateSourceReactJsx } from "@jmassberg/storybook-utils";\n
const reactJsxSource = generateSourceReactJsx(${generatorIntegrationSourceArgs});
`}
/>

### Output

<Source
    dark
    language="jsx"
    code={generateSourceReactJsx(generatorIntegrationArgs)}
/>

## React Function Component

Use `generateSourceReactFC` to generate an example React component integration source.

<Source
    code={`
import { generateSourceReactFC } from "@jmassberg/storybook-utils";\n
const reactFCSource = generateSourceReactFC(${generatorIntegrationSourceArgs});
`}
/>

### Output

<Source
    dark
    language="jsx"
    code={generateSourceReactFC(generatorIntegrationArgs)}
/>
