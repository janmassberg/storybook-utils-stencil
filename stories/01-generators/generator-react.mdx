import { Meta, Source } from "@storybook/blocks";
import { generateSourceCodeReact } from "../../src";
import { testArgsButton } from "../../src/test-utils/testArgs";

export const usageExample = `
import { generateSourceCodeReact } from "@jmassberg/storybook-utils";\n
const reactCode = generateSourceCodeReact("ui-button", {
    required: true,
    variant: "primary",
    onClick: (event) => {
        // Handle click event
        console.log(event.target);
    },
    _slot: "Label",
});
`;

<Meta title="Code Generators/React" />

# React Source Code Generator

## Usage

Use `generateSourceCodeReact` to generate an integration example for React.

<Source code={usageExample} />

## Output

<Source
    dark
    language="jsx"
    code={generateSourceCodeReact("ui-button", testArgsButton)}
/>
