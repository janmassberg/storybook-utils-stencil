import { Meta, Source } from "@storybook/blocks";
import {
    generatorIntegrationArgs,
    generatorIntegrationSourceArgs,
} from "./generator.templates";
import {
    generateSourceAngularComponent,
    generateSourceAngularHtml,
} from "../../src";

<Meta title="Code Generators/Angular" />

# Angular Source Generators

- [generateSourceAngularHtml](#generatesourceangularhtml)
- [generateSourceAngularComponent](#generatesourceangularcomponent)

<br />

## `generateSourceAngularHtml`

<Source
    code={`
import { generateSourceAngularHtml } from "@jmassberg/storybook-utils";\n
const angularHtmlSource = generateSourceAngularHtml(${generatorIntegrationSourceArgs});
`}
/>

### Output

<Source
    dark
    language="html"
    type="code"
    code={generateSourceAngularHtml(generatorIntegrationArgs)}
/>

<br />

## `generateSourceAngularComponent`

<Source
    code={`
import { generateSourceAngularComponent } from "@jmassberg/storybook-utils";\n
const angularComponentSource = generateSourceAngularComponent(${generatorIntegrationSourceArgs});
`}
/>

### Output

<Source
    dark
    language="typescript"
    code={generateSourceAngularComponent(generatorIntegrationArgs)}
/>
