import { Meta, Source } from "@storybook/blocks";
import {
    generateSourceCodeAngularComponent,
    generateSourceCodeAngularHtml,
} from "../../src";
import { testArgs } from "../../src/test-utils/testArgs";

export const usageExample = `
import {
    generateSourceCodeAngularComponent,
    generateSourceCodeAngularHtml,
} from "@jmassberg/storybook-utils-stencil";\n
const args = {
    testFalse: false,
    testTrue: true,
    testString: "test-string",
    testNumber: 12345,
    testArray: [1, 2, 3],
    testObject: {
        foo: "bar",
    },
    onTestEvent: testArgEventHandler,
    _children: "Test children",
};\n
const angularHtmlCode = generateSourceCodeAngularHtml("my-component", args);
const angularComponentCode = generateSourceCodeAngularComponent("my-component", args);
`;

<Meta title="Code Generators/Angular" />

# Angular Source Code Generator

## Usage

<Source code={usageExample} />

## Output

#### Angular Template

Use `generateSourceCodeAngularHtml` to generate the Angualr template source.

<Source
    dark
    language="html"
    type="code"
    code={generateSourceCodeAngularHtml("my-component", testArgs)}
/>

#### Angular Component

Use `generateSourceCodeAngularComponent` to generate the Angualr component source.

<Source
    dark
    language="typescript"
    code={generateSourceCodeAngularComponent("my-component", testArgs)}
/>
