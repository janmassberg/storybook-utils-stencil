import { Meta, Source } from "@storybook/addon-docs";
import {
    filterEventHandlers,
    filterHtmlAttributes,
    filterJsxProperties,
} from "../../src/utils/argUtils";
import { objectToString } from "../../src/utils/objectToString";

export const testArgs = {
    testNull: null,
    testUndefined: undefined,
    testFalse: false,
    testTrue: true,
    testString: "test-string",
    testNumber: 12345,
    testArray: [1, 2, 3],
    testObject: {
        foo: "bar",
    },
    testFunction: () => {},
    onTestEvent: () => {},
};

<Meta title="Utilities/argUtils" />

# Utilities

## argUtils

```typescript
import {
    filterHtmlAtributes,
    filterJsxProperties,
} from "@jmassberg/storybook-utils-stencil";

export const args = {
    testNull: null,
    testUndefined: undefined,
    testFalse: false,
    testTrue: true,
    testString: "test-string",
    testNumber: 12345,
    testArray: [1, 2, 3],
    testObject: {
        foo: "bar",
    },
    testFunction: () => {},
    onTestEvent: () => {},
};

const htmlArgs = filterHtmlAtributes(args);
const jsxProperties = filterJsxProperties(args);
```

### filterHtmlAtributes

<Source
    code={filterHtmlAttributes(testArgs)
        .map(([key, value]) => `${key}: ${objectToString(value)}`)
        .join("\n")}
/>

### filterJsxProperties

<Source
    code={filterJsxProperties(testArgs)
        .map(([key, value]) => `${key}: ${objectToString(value)}`)
        .join("\n")}
/>

### filterEventHandlers

<Source
    code={filterEventHandlers(testArgs)
        .map(([key, value]) => `${key}: ${objectToString(value)}`)
        .join("\n")}
/>
